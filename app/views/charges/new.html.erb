<div class="checkout page-content-wrapper">
  <div class="page-content">
    <%= render partial: "users/section_header", locals: { section_header_title: "Checkout" } %>
    <div class="portlet-body">
      <div class="plans">
      <%= render 'subscriptions/navigation' %>
        <div class="row">
          <div>
            <h3>Cart</h3>
            <%= render "subscriptions/cart" %>
            <li>Total <%= cents_to_currency(@total) %></li>
            <li>Total <%= cents_to_currency(@add_on_charge) %></li>
            <%= button_to "Pay Now", new_charge_path({ amount: @total }) %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>