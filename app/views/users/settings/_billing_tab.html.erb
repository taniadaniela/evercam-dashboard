<div class="row" id="billing">
  <div class="col-md-12">
    <ul class="list-group list-group-lg">
      <li class="list-group-item">
        <div class="panel-section">
          <div class="panel-description col-md-4">
            <h5>Evercam Bundle Silver</h5>

            <div class="row">
              <div class="col-md-6">
                <div class="margin-top-15">
                <%= form_tag charges_path do %>

                  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                          data-description="Evercam Bundle Silver (Annual)"
                          data-amount="19500"></script>
                <% end %>
                </div>
                <p class="margin-top-10 margin-bottom-10">Annual - €199.00</p>
              </div>
              <div class="col-md-6">
                <div class="margin-top-15">
                  <%= form_tag charges_path do %>

                    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                            data-description="Evercam Bundle Silver (Monthly)"
                            data-amount="2500"></script>
                  <% end %>
                </div>
                <p class="margin-top-10 margin-bottom-10">Monthly - €25.00</p>
              </div>
            </div>
          </div>
          <div class="panel-content col-md-8">
            <p class="">Our Silver bundle entitles you to:</p>
            <div class="list">
              <ul class="">
                <li>
                  Live Streaming
                </li>
                <li>
                  Recording for up to 5 cameras
                </li>
                <li>
                  Our Snapmail app
                </li>
                <li>
                  Our Timelapse app
                </li>
              </ul>
            </div>
          </div>
        </div>
      </li>
      <li class="list-group-item">
        <div class="panel-section">
          <div class="panel-description col-md-4">
            <h5>Snapmail</h5>

            <div class="row">
              <div class="col-md-6">
                <div class="margin-top-15">
                  <%= form_tag charges_path do %>

                    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                            data-description="Evercam Bundle Silver (Monthly)"
                            data-amount="9900"></script>
                  <% end %>
                </div>
                <p class="margin-top-10 margin-bottom-10">Annual - €99.00</p>
              </div>
              <div class="col-md-6">
                <div class="margin-top-15">
                  <%= form_tag charges_path do %>

                    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                            data-description="Snapmail (Monthly)"
                            data-amount="1000"></script>
                  <% end %>
                </div>
                <p class="margin-top-10 margin-bottom-10">Montly - €10.00</p>
              </div>
            </div>
          </div>
          <div class="panel-content col-md-8">

          </div>
        </div>
      </li>
      <li class="list-group-item">
        <div class="panel-section">
          <div class="panel-description col-md-4">
            <h5>Timelapse</h5>

            <div class="row">
              <div class="col-md-6">
                <div class="margin-top-15">
                  <%= form_tag charges_path do %>

                    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                            data-description="Timelapse (Annual)"
                            data-amount="9900"></script>
                  <% end %>
                </div>
                <p class="margin-top-10 margin-bottom-10">Annual - €99.00</p>
              </div>
              <div class="col-md-6">
                <div class="margin-top-15">
                  <%= form_tag charges_path do %>

                    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
                            data-description="Timelapse (Annual)"
                            data-amount="1000"></script>
                  <% end %>
                </div>
                <p class="margin-top-10 margin-bottom-10">Montly - €10.00</p>
              </div>
            </div>
          </div>
          <div class="panel-content col-md-8">

          </div>
        </div>
      </li>

    </ul>
  </div>
</div>
